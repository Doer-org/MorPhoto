import { style } from "@vanilla-extract/css";
import { blueDark } from "@radix-ui/colors";

export const titleStyle = style({
  fontSize: 42,
  fontWeight: 700,
});

export const titleBlueStyle = style({
  color: blueDark.blue7,
});
